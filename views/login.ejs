<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Login Page</title>
    <style>
        @keyframes shake {
            0% { transform: translateX(0); }
            25% { transform: translateX(-5px); }
            50% { transform: translateX(5px); }
            75% { transform: translateX(-5px); }
            100% { transform: translateX(0); }
        }
        .shake {
            animation: shake 0.5s ease-in-out;
        }
    </style>
</head>
<body class="w-full h-screen bg-zinc-900 text-white flex justify-center items-center p-10">
    <div class="w-full max-w-md">
        <h3 class="text-3xl mb-5 text-center">Login Your Account</h3>

        <!-- Error Message Display -->
        <% if (errorMessage) { %>
            <div class="bg-red-500 text-white p-4 mb-4 rounded-md shake text-center">
                <p><%= errorMessage %></p>
            </div>
        <% } %>

        <form method="post" action="/login" class="space-y-4">
            <input class="px-3 py-2 w-full rounded-md bg-transparent border-2 border-zinc-800 outline-none" type="email" name="email" placeholder="Email" required>
            <input class="px-3 py-2 w-full rounded-md bg-transparent border-2 border-zinc-800 outline-none" type="password" name="password" placeholder="Password" required>
            <input class="px-5 py-2 w-full rounded-md bg-blue-500 hover:bg-blue-600 cursor-pointer" type="submit" value="Login Account">
        </form>
    </div> 
</body>
</html>
